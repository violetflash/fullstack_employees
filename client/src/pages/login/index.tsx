// @flow
import * as React from 'react';
import { AppLayout } from '../../components/layout';

type Props = {

};
export const Login = (props: Props) => {
  return (
    <AppLayout>
      Войти
    </AppLayout>
  );
};